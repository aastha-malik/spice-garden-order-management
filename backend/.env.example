# Postgres published by docker-compose on 5433 (5432 is often taken locally)
DATABASE_URL=postgres://spice:spice@localhost:5433/spice_garden
PORT=3000
# Comma-separated list of origins allowed to call the API
CORS_ORIGIN=http://localhost:5173
